# Repository rules

## Git identity

- Use only `aiwenjie777 <kevinhh284@gmail.com>` for this repository.
- Before committing or pushing, run `./scripts/git-identity-guard.sh check`.
- Never use another author, email, or a `Co-authored-by` trailer in new commits.
- Keep identity configuration repository-local; do not change the user's global Git identity.
- Do not rewrite published history without explicit user approval.
